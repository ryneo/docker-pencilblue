# pencilblue

## Note: This is a work in progress

# Supported tags and respective `Dockerfile` links

# How to use

`docker run -P -d --link my-mongodb:db ryneo/pencilblue:beta nodemon pencilblue`

## Configuration

# Supported Docker versions
